version: '3.8'
services:
  app:
    build: .
    container_name: kitap_fark_flask
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    working_dir: /app/app.py
    command: flask run --host=0.0.0.0 --port=5000
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production 